[project]
name = "investmenteda"
version = "0.1.0"
description = "Local commodity and supply-chain control room (v0)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "httpx>=0.28.1",
    "pandas>=3.0.5",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "streamlit>=1.63.0",
]
